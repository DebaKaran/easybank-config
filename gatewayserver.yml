eureka:
  instance:
    preferIpAddress: true
  client:
    register-with-eureka: true
    fetch-registry: true

info:
  app:
    name: "gatewayserver"
    description: "Eazy Bank Gateway Server"
    version: "1.0.0"
